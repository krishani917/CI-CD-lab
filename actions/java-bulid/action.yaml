name: "Java Build Action"

description: "Compile and run Java program"
inputs:
 file name: 
   description: "Java file name"
   required: true

runs:
 using: "Composite"

 steps:
 -name: Compile Java File
 run: javac ${{add.Main}}.java

 shell: bash
 -name: Run Java Program
 run: java ${{add.Main}}
 shell: bash